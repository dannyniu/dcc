_Include "./some-decl.hxing";

extern subr recusion2(x);
subr recursion1(x)
{
    if( x % 2 == 1 ) return recursion1(x * 3 + 1);
    else return recursion2(x);
}
subr recursion2(x)
{
    return recursion1(x / 2);
}

subr message()
{
    decl msg = "We'll spend forever to find out.\n";
    return msg;
}

subr fpcalc(s, t)
{
    decl u = 01 + 23 + 4.5 + 67e+89 + 0x1p-2 + .34;
    decl v = (1 + 3) * (2 + 4);
    decl w = u + s * v;
    !isnull(w) and return w;
    return t > 3 ? u*u - v*v : t > 1 ? u*v - 2 : t=?9>0 ? u/v =? 12 : null;
}